```agda
open import 1Lab.Prelude

open import 1Lab.Path
open import 1Lab.Equiv

module CPup.W1 where

w1 :
  {ℓ : Level} {A : Type ℓ} {a b : A} →
  Path A a b → (i : I) → Sub A (~ i ∨ i) (λ{ (i = i0) → a; (i = i1) → b })
w1 p i = inS (p i)

w2 :
  {ℓ : Level} {A : Type ℓ} {a b : A} →
  ((i : I) → Sub A (~ i ∨ i) (λ{ (i = i0) → a; (i = i1) → b })) →
  Path A a b
w2 s i = outS (s i)

w3 :
  {ℓ : Level} {A : Type ℓ} {a b : A} →
  (p : Path A a b) →
  w2 (w1 p) ≡ p
w3 p i j = p j

```

# CoderPuppy's test page 1
